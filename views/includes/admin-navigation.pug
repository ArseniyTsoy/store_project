header.header
  div.flex
    a.logo(href="/admin") 
      | Admin
      span 
        | Panel
    nav.navbar
      a(href="/") В магазин
      a(href="/admin/products") Товары
      a(href="/admin/orders") Заказы
      a(href="/admin/users") Пользователи
      a(href="/admin/contacts") Сообщения
    div.icons
      div.fa-solid.fa-bars#menu-btn
      div.fa-solid.fa-user#user-btn    
    div.profile
      img(src="/" + user.imageUrl, alt=user.name)
      p #{user.name}
      a.btn(href="/admin/update-profile") Update profile
      form(action="/auth/logout", method="POST")
        input(type="hidden", name="_csrf", value=csrfToken)
        button.delete-btn(type="submit") Выйти
      div.flex-btn
        a.option-btn(href="/login") Логин
        a.option-btn(href="/register") Регистрация